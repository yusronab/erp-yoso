const Loading = () => {
    return (
        <div className="w-full h-full flex justify-center">
            <div className="loader">
                <span>Loading</span>
                <span>Loading</span>
            </div>
        </div>
    );
};

export default Loading;